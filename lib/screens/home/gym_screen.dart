import 'package:flutter/material.dart';

class GymWorkoutScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Gym Workouts'),
      ),
      body: Center(
        child: Text('Content for Gym Workouts'),
      ),
    );
  }
}
